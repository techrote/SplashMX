{
  "schema": "splashmx.smx038-godot-runtime-fixtures/1",
  "issue": "SMX-038",
  "architecture_version": "1.0",
  "cases": [
    {"id": "GRT-001", "claim": "same semantic projection prepares native/browser/headless profiles"},
    {"id": "GRT-002", "claim": "missing required target feature fails before materialization"},
    {"id": "GRT-003", "claim": "optional unsupported target features degrade explicitly"},
    {"id": "GRT-004", "claim": "unknown target feature fails closed"},
    {"id": "GRT-005", "claim": "duplicate ThingId is rejected"},
    {"id": "GRT-006", "claim": "undeclared Thing facet is rejected"},
    {"id": "GRT-007", "claim": "competing protected Asset revision is rejected"},
    {"id": "GRT-008", "claim": "NodePath injection is rejected recursively"},
    {"id": "GRT-009", "claim": "RID and ResourceUID injection are rejected recursively"},
    {"id": "GRT-010", "claim": "transport/process identity injection is rejected recursively"},
    {"id": "GRT-011", "claim": "non-finite adapter values are rejected"},
    {"id": "GRT-012", "claim": "semantic-only Thing receives zero private objects"},
    {"id": "GRT-013", "claim": "binding recreation preserves ThingId and semantic view"},
    {"id": "GRT-014", "claim": "candidate materialization failure preserves prior published bindings"},
    {"id": "GRT-015", "claim": "private binding-object budget fails closed"},
    {"id": "GRT-016", "claim": "private binding-count budget fails closed"},
    {"id": "GRT-017", "claim": "engine callback capture does not execute Behaviour directly"},
    {"id": "GRT-018", "claim": "explicit semantic event order is independent of callback arrival order"},
    {"id": "GRT-019", "claim": "normalized engine event rejects transient handles"},
    {"id": "GRT-020", "claim": "normalized input queue is independently bounded"},
    {"id": "GRT-021", "claim": "SMX-026 scheduler queue budget remains authoritative"},
    {"id": "GRT-022", "claim": "render/audio/input/physics host services have distinct capability IDs"},
    {"id": "GRT-023", "claim": "host effect occurs only after SMX-027 admission and final-use authorization"},
    {"id": "GRT-024", "claim": "revocation after admission prevents host effect"},
    {"id": "GRT-025", "claim": "missing capability denies host effect"},
    {"id": "GRT-026", "claim": "capability scope cannot be confused across adapter families"},
    {"id": "GRT-027", "claim": "service payload cannot smuggle Godot handle"},
    {"id": "GRT-028", "claim": "unsafe host result is rejected by host-service boundary"},
    {"id": "GRT-029", "claim": "media derivative is keyed to exact protected Asset revision"},
    {"id": "GRT-030", "claim": "competing/unknown Asset revisions cannot populate derivative cache"},
    {"id": "GRT-031", "claim": "derivative eviction does not change canonical protected Asset meaning"},
    {"id": "GRT-032", "claim": "native/browser/headless profile preparation preserves exact AssetId+revision digest"}
  ],
  "protected_media_fields": [
    "digest",
    "source_identity",
    "source_metadata",
    "audio_media_semantics",
    "provenance",
    "licence_attribution",
    "derivation_lineage"
  ],
  "forbidden_identity_classes": [
    "NodePath",
    "RID",
    "ResourceUID",
    "resource_path",
    "DOM_node_identity",
    "database_row_id",
    "cache_key",
    "url",
    "transport_peer_id",
    "connection_handle",
    "socket_id",
    "session_id",
    "process_handle"
  ]
}
